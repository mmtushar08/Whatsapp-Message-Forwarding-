{
  "name": "@whatsapp-forwarder/shared",
  "version": "1.0.0",
  "description": "Shared utilities for WhatsApp Forwarder monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "@types/node": "^20.10.0"
  }
}
